class LSTMPriceForecaster:
    """Phase 1 stub. Replace with a trained sequence model in Phase 7."""
    name='lstm'
    def predict(self, series): return {'model':self.name,'status':'stub','forecast':[]}
