class HMMRegimeDetector:
    """Phase 1 stub. Replace with trained HMM inference in Phase 8."""
    name='hmm'
    def predict(self, features): return {'model':self.name,'status':'stub','regime':'UNKNOWN'}
